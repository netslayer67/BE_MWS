version: "3.8"
services:
  api:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env.docker
    restart: unless-stopped
    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs
