version: "3.8"
services:
  api:
    build: .
    ports:
      - "3003:3003"
    env_file:
      - .env.docker
    restart: unless-stopped
    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs
